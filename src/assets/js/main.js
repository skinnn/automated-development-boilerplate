console.log('Hello JavaScript!');

// You can see the ES5 equivalent of this arrow function after compiling
// with Babel (npm run build), located in dist/assets/js/main.js

[1, 2, 3].map((n) => n + 1);